import { mount, shallowMount } from '@vue/test-utils'
import Index from '../pages/index.vue'

describe('Index.vue', () => {
    let wrapper;

    beforeEach(async () => {
        wrapper = mount(Index);
    });

    it.todo('Should work');
})